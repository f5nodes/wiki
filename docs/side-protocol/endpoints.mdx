---
sidebar_position: 1
description: Endpoints
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Endpoints — API / RPC / gRPC

> In the Cosmos SDK, the gRPC-gateway translates gRPC endpoints into RESTful ones, enabling users to interact via the REST API. This adaptation is essential for clients that don't natively support gRPC or HTTP2.

> The RPC endpoint enables users to interact with Side's nodes, querying the node's state and transmitting transactions within the Side Protocol.

> The gRPC endpoint enables user interactions with a Side Node through the efficient gRPC framework. In the Cosmos SDK, this protocol is crucial for state queries and transaction broadcasts.


<Tabs>
  <TabItem value="api" label="API" default>

#### AkNodes:
```bash
https://api.side.aknodes.net
```

#### ItRocket:
```bash
https://side-testnet-api.itrocket.net
```

#### kjnodes:
```bash
https://side-testnet.api.kjnodes.com
```

#### Lavender.Five:
```bash
https://testnet-side-api.lavenderfive.com
```

#### Nodeist:
```bash
https://api-testnet-side.nodeist.net
```

#### Nodejumper:
```bash
https://rest.nodejumper.io/sidetestnet
```

#### NodeStake:
```bash
https://api-t.side.nodestake.org
```

#### Stake Towm:
```bash
https://side-testnet-api.stake-town.com:443
```

#### Synarge Nodes:
```bash
https://side-testnet-api.synergynodes.com
```

#### vinjian:
```bash
https://api-side.vinjan.xyz
```

</TabItem>
  <TabItem value="rpc" label="RPC">

#### AkNodes:
```bash
https://rpc.side.aknodes.net
```

#### ItRocket:
```bash
https://side-testnet-rpc.itrocket.net
```

#### kjnodes:
```bash
https://side-testnet.rpc.kjnodes.com
```

#### Lavender.Five:
```bash
https://testnet-side-rpc.lavenderfive.com
```

#### Nodeist:
```bash
https://rpc-testnet-side.nodeist.net
```

#### Nodejumper:
```bash
https://rpc.nodejumper.io:443/sidetestnet
```

#### NodeStake:
```bash
https://rpc-t.side.nodestake.org
```

#### Stake Towm:
```bash
https://side-testnet-rpc.stake-town.com
```

#### Synarge Nodes:
```bash
https://side-testnet-rpc.synergynodes.com
```

#### vinjian:
```bash
https://rpc-side.vinjan.xyz
```

</TabItem>
  <TabItem value="grpc" label="gRPC">

#### AkNodes:
```bash
grpc.side.aknodes.net:11390
```

#### ItRocket:
```bash
side-testnet-grpc.itrocket.net:45090
```

#### kjnodes:
```bash
side-testnet.grpc.kjnodes.com:443
```

#### Lavender.Five:
```bash
testnet-side-grpc.lavenderfive.com:443
```

#### Nodeist:
```bash
grpc-testnet-side.nodeist.net:443
```

#### NodeStake:
```bash
grpc-t.side.nodestake.org:443
```

#### Stake Towm:
```bash
side-testnet-grpc.stake-town.com:443
```

#### Synarge Nodes:
```bash
side-testnet-grpc.synergynodes.com:443
```

</TabItem>
</Tabs>